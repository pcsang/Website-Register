/**
 * Development environment configuration, used by `ng serve` / `npm start`
 * (swapped in via `fileReplacements` for the `development` build configuration).
 */
export const environment = {
  production: false,
  apiBaseUrl: 'http://localhost:8080'
};

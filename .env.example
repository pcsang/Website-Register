# Copy to .env to override docker-compose.yml's dev-only defaults. Never commit the real .env file.

POSTGRES_DB=information_db
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

JWT_SECRET=dev-only-insecure-jwt-signing-secret-CHANGE-ME-before-any-real-deploy-32bytes
ALLOWED_ORIGINS=http://localhost:4200
